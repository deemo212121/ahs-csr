import { defineCloudflareConfig } from "@opennextjs/cloudflare";

export default defineCloudflareConfig({
	// Default OpenNext Cloudflare configuration. Server externals are handled
	// in `next.config.ts` via `serverExternalPackages`.
});
